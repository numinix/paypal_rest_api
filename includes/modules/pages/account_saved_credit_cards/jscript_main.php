<?php
/**
 * Legacy PayFlow add-card form validation.
 *
 * Intentionally left blank: the account saved-cards page now uses PayPal Vault
 * CardFields hosted inputs (see paypalac_render_card_field_containers), so the
 * old form-check helpers are unused. Keeping an empty file avoids CJ Loader
 * gaps while preventing stale inline JS from breaking the page.
 */
