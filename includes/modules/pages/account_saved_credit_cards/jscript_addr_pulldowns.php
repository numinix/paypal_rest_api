<?php
/**
 * Legacy state/country pulldown helpers for the old PayFlow add-card form.
 *
 * Intentionally left blank: the Vault add/edit card UI no longer uses zone_id /
 * zone_country_id fields. The previous zen_js_zone_list() output could also emit
 * SyntaxError when zone names contained quotes, blocking later page scripts.
 */
